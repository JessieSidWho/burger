<li>
	{{burger_name}} <br>

	<button class="change-devour btn btn-secondary" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}I'M FULL!{{else}}EAT!{{/if}}
	</button>

	<button class="delete-burger btn btn-secondary" data-id="{{this.id}}">DELETE!</button>
</li> <br>